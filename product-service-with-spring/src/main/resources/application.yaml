logging:
  level:
    com.github.andreluizdev12.product_with_spring.service: DEBUG

server:
  port: 8000
spring:
  application:
    name: product-with-spring
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: 12
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

